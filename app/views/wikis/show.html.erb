<div class="media-body">
  <h4 class="media-heading">
    <%= @wiki.name %>
  </h4>
  <small>

    <%= @wiki.description %>
  </small>
  <% if policy(@wiki).edit? %>
  <%= link_to "Edit", edit_wiki_path(@wiki.id) %>
  <% end %>
</div>